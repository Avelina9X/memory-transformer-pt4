meta:
  group:
    variant: 4x2

model:
  pooler_config:
    token_pooling_config:
      attn_layers:
        - pool
        - cross
        - pool
        - cross
      
      # norm: both
      # head_gate: false

      n_repeats: 2